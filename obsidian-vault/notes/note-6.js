string.charCodeAt(index) - returns UTF-16 code unit at the given index
string.concat(string1,string2) - concatenate one or multiple strings to the string
string.endWith(string1) - returns true if string1 is at end of string
string.startsWith(string1) - returns true if string1 is at beginning of string 
string.includes(string1) - returns true if string contain string1
string.indexOf(string1,indexOfFirst + 1) - returns the index of first ocurrence of the string or returns the index of the next ocurrence
string.lastIndexOf(string1) - returns the index of the last ocurrence
string.match(regexp) - returns an array matching the regex pattern
string.replace(string1,replacement) - replace the first ocurence string with the replacement
string.replaceAll(string1,replacement) - replace all ocurrence of the string with the replacement
string.slice(indexStart|indexStart,indexEnd) - extract a section of a string it includes indexStart but it not includes indexEnd because it removes the indexEnd
string.split(separator|regexp) - divides the string into an array using a pattern
string.toLowerCase() - convert string to LowerCase
string.toUpperCase() - convert string to UpperCase
string.trim() - removes whitespace from both ends