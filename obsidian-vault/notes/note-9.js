Array.concat(array1,array2,arra3...) - concatenate arrays its returns a new arrray
Array.every(callbackFunction(value,index,array)) - return false if it finds an element does not satisfy the test
Array.filter(callbackFunction(value,index,array)) - return an array with elements that passed the test
Array.find(callbackFunction(value,index,array)) - return the first element that passed the test
Array.forEach(callbackFunction(value,index,array)) - execute a function for each element
Array.includes(element) - return true if includes the element in the array , if no element is passed return false
Array.join(separator) - returns a string concatenating all elements of the array using a separator
Array.map(callbackFunction(value,index,array)) - changing all elements and return the array
Array.pop() - removes the last element 
Array.shift() - removes the first element
Array.push(element) - add new elemenet to the end and returns new length of the array 
Array.indexOf(element) - return the index of the first element that match element
Array.slice(indexStart,indexEnd) - return a copy of the array that includes indexSart and not include indexEnd , The original array is not modified 
Array.some(callbackFunction(value,index,array)) - returns true if find an element that satisfy the test